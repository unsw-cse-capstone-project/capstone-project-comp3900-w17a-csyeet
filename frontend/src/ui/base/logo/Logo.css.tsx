import { createStyles, makeStyles } from "@material-ui/core";
export const LogoStyles = makeStyles(() => {
  createStyles({
    small: {
      height: "60px",
      width: "120px",
    },
    large: {
      height: "250px",
      width: "500px",
    },
  });
});
